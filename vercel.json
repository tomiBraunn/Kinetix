{
  "buildCommand": "npm run build --prefix front",
  "installCommand": "npm install --prefix front && npm install --prefix back",
  "outputDirectory": "front/dist",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
